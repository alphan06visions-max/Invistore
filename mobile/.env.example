# Env pour dev local (crée un fichier `.env` à côté)
EXPO_PUBLIC_API_URL=https://nexus-backend.fly.dev
EXPO_PUBLIC_WS_URL=wss://nexus-backend.fly.dev/ws
# AGORA_APP_ID is fetched dynamically from the backend (api/call/token),
# so you don't need to set it here. Just configure it in the backend.
